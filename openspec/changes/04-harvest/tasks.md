# Tasks: 采收与产后

## 1. 数据模型

- [ ] 1.1 建表 `harvest`；验证 DDL 可执行

## 2. 采收 API

- [ ] 2.1 实现采收记录 CRUD；验证单测通过（对应 spec 采收场景）
- [ ] 2.2 实现产量统计（按批次/地块/作物聚合）；验证统计单测

## 3. 前端与验收

- [ ] 3.1 `apps/web` 采收记录与统计页；验证闭环
- [ ] 3.2 后端 TDD 全绿；验证 `mvn test` 通过
- [ ] 3.3 端到端验收：与 spec 全部 Scenario 一致
