# Tasks: 设备接入

## 1. 数据模型

- [ ] 1.1 建表 `iot_base`、`iot_device`、`device_control_log`；验证 DDL 可执行

## 2. 基地与授权

- [ ] 2.1 实现基地绑定与授权凭证加密存储；验证单测通过（对应 spec 基地/授权场景）
- [ ] 2.2 实现 `DeviceProvider` 接口 + mock 适配器；验证 mock 可用

## 3. 设备与控制

- [ ] 3.1 实现设备台账 CRUD；验证单测通过（对应 spec 设备场景）
- [ ] 3.2 实现代理转发查询状态；验证单测通过（对应 spec 转发场景）
- [ ] 3.3 实现控制指令下发（审计 + 幂等 + 权限校验）；验证幂等与审计测试

## 4. 前端与验收

- [ ] 4.1 `apps/web` 设备台账与基地管理页；验证闭环
- [ ] 4.2 客户端地图渲染设备点位（Leaflet 叠加）；验证展示
- [ ] 4.3 后端 TDD 全绿；验证 `mvn test` 通过
- [ ] 4.4 端到端验收：与 spec 全部 Scenario 一致
