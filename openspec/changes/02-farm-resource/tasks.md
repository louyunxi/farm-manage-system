# Tasks: 农场资源

## 1. 数据模型

- [ ] 1.1 建表 `farm`、`plot`（含 `geom`/`geojson`/`area_mu`）、`batch`；验证 DDL 可执行
- [ ] 1.2 接入 JTS 多边形校验工具类；验证自相交/未闭合样例被拒

## 2. 农场 API

- [ ] 2.1 实现农场 CRUD（tenant_id 自动注入）；验证单测通过（对应 spec 农场场景）

## 3. 地块 API

- [ ] 3.1 实现地块保存（GeoJSON + 面积，JTS 校验）；验证合法/非法多边形单测
- [ ] 3.2 实现更新轮廓触发面积重算；验证面积字段随轮廓变化更新

## 4. 批次 API

- [ ] 4.1 实现批次创建与按农场筛选；验证单测通过（对应 spec 批次场景）

## 5. 前端与验收

- [ ] 5.1 `apps/web` Leaflet 地块绘制/编辑页（含 Turf 面积计算）；验证保存闭环
- [ ] 5.2 后端 TDD 全绿；验证 `mvn test` 通过
- [ ] 5.3 端到端验收：与 spec 全部 Scenario 一致
