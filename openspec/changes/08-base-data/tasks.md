# Tasks: 基础数据

## 1. 数据模型

- [ ] 1.1 建表 `dict_type`、`dict_item`、`variety`、`input_catalog`（UUID 主键 `*_id`、`is_deleted`、枚举 text+CHECK）；验证 DDL 可执行
- [ ] 1.2 将四张表加入多租户插件白名单；验证查询不注入 `tenant_id`（单测断言）

## 2. 字典 API

- [ ] 2.1 实现字典类型与字典项 CRUD；验证创建/编辑/停用单测通过（对应 spec 字典场景）
- [ ] 2.2 实现字典全量查询接口；验证任意租户返回平台级完整字典

## 3. 品种 API

- [ ] 3.1 实现品种新增与按作物类别筛选；验证单测通过（对应 spec 品种场景）

## 4. 农资目录 API

- [ ] 4.1 实现农资目录新增与按类别筛选；验证单测通过（对应 spec 农资目录场景）

## 5. 前端与验收

- [ ] 5.1 `apps/web` 字典/品种/农资目录管理页；验证可完成 CRUD 闭环
- [ ] 5.2 后端 TDD 全绿（字典/品种/农资目录三组）；验证 `mvn test` 通过
- [ ] 5.3 端到端验收：与 spec 全部 Scenario 一致
