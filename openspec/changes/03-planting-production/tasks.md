# Tasks: 种植生产

## 1. 数据模型

- [ ] 1.1 建表 `farm_operation`、`farm_operation_plot`、`crop_plan`、`plan_item`；验证 DDL 可执行

## 2. 农事 API

- [ ] 2.1 实现单条农事记录（一主一从）；验证单测通过（对应 spec 单条场景）
- [ ] 2.2 实现批量农事记录（一主多从，事务）；验证批量与回滚单测
- [ ] 2.3 实现按地块/批次/时间筛选；验证筛选单测

## 3. 种植计划 API

- [ ] 3.1 实现种植计划 CRUD；验证单测通过（对应 spec 计划场景）
- [ ] 3.2 实现投入品预估明细；验证单测通过
- [ ] 3.3 实现成本预算汇总；验证金额计算单测

## 4. 前端与验收

- [ ] 4.1 `apps/web` 农事记录页（含多地块批量入口）；验证闭环
- [ ] 4.2 `apps/web` 种植计划页（预估 + 预算）；验证闭环
- [ ] 4.3 后端 TDD 全绿；验证 `mvn test` 通过
- [ ] 4.4 端到端验收：与 spec 全部 Scenario 一致
