# 任务清单总览

> 任务清单位于 `docs/tasks/<编号>-<域名>/tasks.md`：`T-<域编号>-<序号>` 编号、0.5~2 天粒度、五态状态（`[ ]`/`[~]`/`[x]`/`[!]`/`[-]`）。
> 当前执行清单在各 openspec change 的 `tasks.md` 中；本目录在实现各域时同步。

## 当前状态

尚无域级任务清单——实现 `01-tenant-auth` 时生成第一份。
